# Нативный клиент macOS

Документ обновлён после проверки клиента `0.60.1289` 29 августа 2026 года.

## Поддерживаемая установка

Официальный лаунчер устанавливает игру сюда:

```text
~/Library/Application Support/zone.hitzone.invokers.launcher/game/Invokers.app
```

Нативный клиент хранит скачанные таблицы локализации вне подписанного приложения:

```text
~/Library/Application Support/hitzone.anima.spirit.guardians/i18n/
```

Для версии `0.60.1289` проверены `dl_en_US.bin` и `dl_uk_UA.bin`. Оба имеют stamp `0.60.1289`, формат LOC1 schema 4 и 41 292 записи. Сборка текущего каталога дала:

- 40 997 русских строк;
- 41 безопасный английский fallback;
- 254 значения из базовой таблицы;
- 40 `stale-hint`, не блокирующих сборку;
- 1 отсутствующее сопоставление.

## Причина сбоя старого установщика

Версия 2.7.0 всегда предпочитала старый контейнер iOS-on-Mac:

```text
~/Library/Containers/<UUID>/Data/Documents/i18n/
```

После выхода нативного клиента этот каталог остался на версии `0.60.1247`, а игра стала читать новый каталог Application Support. Патчер сообщал об успешной установке, но менял неиспользуемый файл.

Версия 3.0:

- проверяет нативный каталог первым и сравнивает stamps всех найденных кешей;
- выбирает наиболее новую версию, а при равенстве предпочитает нативный клиент;
- показывает выбранный клиент, версию и путь до изменения;
- хранит отдельное состояние и резервную копию для каждого кеша;
- распознаёт процесс нативной игры и не пишет файл во время её работы.

Переменная `INVOKERSRU_CACHE_ROOT` остаётся явным override для тестирования и нестандартных установок.

## Почему используется `uk_UA`

Русский текст подставляется в украинский слот: это кириллическая локаль, уже поддерживаемая шрифтами игры. До установки игрок должен выбрать украинский язык и дождаться появления `dl_uk_UA.bin`. После установки выбор языка открывать нельзя — клиент перекачивает официальный файл.

Запуск с `-language en_US` показывает английский независимо от содержимого украинского файла и при выходе сохраняет locale id `1`. Это не ошибка сборки перевода; активным должен оставаться `uk_UA`. Версия 3.0 сохраняет id `8` после установки, а кнопка запуска открывает нативную игру с `-language uk_UA`.

## Безопасность изменения

Приложение не меняет `Invokers.app`. Оно заменяет только `dl_uk_UA.bin` в пользовательском кеше, предварительно создавая content-addressed backup и сверяя SHA-256. Запись выполняется атомарно; восстановление допускается только из зафиксированной резервной копии.

Preferences клиента могут содержать токены аккаунта, идентификаторы и email. Их нельзя добавлять в репозиторий, журнал или issue.

## Проверки

```bash
scripts/test-mac-patcher.sh
scripts/build-mac-app.sh
```

Первый тест проверяет выбор между нативным и старым кешем, override и изоляцию state. Сборщик создаёт универсальное Cocoa-приложение `arm64 + x86_64`, включает self-contained CLI и формирует DMG.
